<% if cart.present? %>
  <ul>
    <% cart.each do |product| %>
      <li>
        <%= product.name %><%= button_to '✖️', cart_path(product), method: :delete %>
      </li>
    <% end %>
  </ul>
<% else %>
  <p>No items in your cart.</p>
<% end %>
